export const environment = {
  production: false,
  // Matches the port your API prints in its "Now listening on" log.
  // If you ran the DLL directly (not dotnet run), that's usually http://localhost:5000.
  apiUrl: 'http://localhost:5000/api'
};
